#include <string>
#include <vector>
using namespace std;

int solution(string my_string, string is_suffix)
{
    int answer = 0;
    int index = my_string.rfind(is_suffix);

    if (index != -1 && my_string.size() - is_suffix.size() == index)
        answer = 1;
    
    return answer;
}
