#include <string>
#include <vector>
using namespace std;

// 실수 flo가 매개 변수로 주어질 때, flo의 정수 부분을 return하도록 solution 함수를 완성해주세요.
int solution(double flo)
{
    int answer = (int)flo;
    return answer;
}
