#include <iostream>
using namespace std;

int main()
{
	int T;
	cin >> T;

	for (int i = 0; i < T; i++)
	{
		int N, A, B;
		cin >> N;
		cin >> A >> B;

		for (int j = 0; j < N; j++)
		{
			int size, weight;
			cin >> size >> weight;
		}

		cout << "Material Management " << i + 1 << endl;
		cout << "Classification ---- End!" << endl;
	}
}